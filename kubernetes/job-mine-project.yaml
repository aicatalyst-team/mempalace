apiVersion: batch/v1
kind: Job
metadata:
  name: mempalace-mine-project
  namespace: mempalace
  labels:
    app.kubernetes.io/name: mempalace
    app.kubernetes.io/component: mine-project
    app.kubernetes.io/managed-by: autopoc
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 120
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mempalace
        app.kubernetes.io/component: mine-project
    spec:
      imagePullSecrets:
        - name: quay-aicatalyst-pull
      containers:
        - name: mempalace
          image: quay.io/aicatalyst/mempalace-mempalace:latest
          imagePullPolicy: Always
          command: ["/bin/sh", "-c"]
          args:
            - >-
              mkdir -p /tmp/sample-project &&
              printf '# Architecture\nWe use GraphQL for the API layer and PostgreSQL for persistence.\n' > /tmp/sample-project/ARCHITECTURE.md &&
              printf '# Setup\nInstall dependencies with npm install, then run npm start.\n' > /tmp/sample-project/SETUP.md &&
              mempalace init /tmp/sample-project &&
              mempalace mine /tmp/sample-project
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
          volumeMounts:
            - name: palace-data
              mountPath: /opt/app-root/.mempalace
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
      volumes:
        - name: palace-data
          emptyDir: {}
      restartPolicy: Never
