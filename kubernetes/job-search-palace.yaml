apiVersion: batch/v1
kind: Job
metadata:
  name: mempalace-search-palace
  namespace: mempalace
  labels:
    app.kubernetes.io/name: mempalace
    app.kubernetes.io/component: search-palace
    app.kubernetes.io/managed-by: autopoc
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 120
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mempalace
        app.kubernetes.io/component: search-palace
    spec:
      imagePullSecrets:
        - name: quay-aicatalyst-pull
      containers:
        - name: mempalace
          image: quay.io/aicatalyst/mempalace-mempalace:latest
          imagePullPolicy: Always
          command: ["/bin/sh", "-c"]
          args:
            - >-
              mkdir -p /tmp/sample-project &&
              printf '# Architecture\nWe switched to GraphQL for the API layer because REST was too verbose.\n' > /tmp/sample-project/ARCHITECTURE.md &&
              mempalace init /tmp/sample-project &&
              mempalace mine /tmp/sample-project &&
              mempalace search 'GraphQL'
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
          volumeMounts:
            - name: palace-data
              mountPath: /opt/app-root/.mempalace
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
      volumes:
        - name: palace-data
          emptyDir: {}
      restartPolicy: Never
