apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mempalace-data
  namespace: mempalace
  labels:
    app.kubernetes.io/name: mempalace
    app.kubernetes.io/managed-by: autopoc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
